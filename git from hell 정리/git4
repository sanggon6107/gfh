merge와 conflict :
	같은 부분을 수정한 두 branch를 merge하려고 하면 충돌이 일어난다.

	병합을 전문적으로 해주는 tool이 있으므로, 코드 수정을 직접 해도 되지만 tool을 사용하면 편리하다.

	git config --global merge.tool kdiff3 로 kdiff3 설치 가능.

	충돌 시 git mergetool 로 kdiff3 실행.





3way merge :
	base 코드가 있고, 나와 다른사람이 각각 다르게 A, B, C, D 코드를 수정했다고 하자.

	Me / Base / Other / 2 way merge / 3 way merge
	A  /  A /       /     ?       /				// 2 way는 me와 other만 비교하기 때문에 무엇이 변경됐는지 알 수 없다. -> 충돌.
	B  /  B /   B   /     B       /     B
	1  /  C /   2   /     ?       /     ?
	   /  D /   D   /     ?       /     

	* ? : 충돌


	3 way merge 는 Base, me, other 전부 보고, base와 비교해서 달라진 쪽을 채택한다.
	2 way merge 는 me, other만 보고 두 코드가 다르면 충돌이 일어난다.


	원격 저장소 :
		로컬 저장소에서 원격 저장소로 push한다.
